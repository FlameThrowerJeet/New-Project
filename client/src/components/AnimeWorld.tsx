import React from 'react';
import IPhoneImageComparison from './iPhoneImageComparison';

// AnimeWorld wraps the previous image-comparison prototype under a new name
const AnimeWorld: React.FC = () => {
  return <IPhoneImageComparison />;
};

export default AnimeWorld; 